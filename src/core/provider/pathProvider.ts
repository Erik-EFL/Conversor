const docxPath = 'public/assets/docx';
const htmlPath = 'public/assets/html';

type path = {
  DocxPath: string;
  HtmlPath: string;
}

export const directory: path = {
  DocxPath: docxPath,
  HtmlPath: htmlPath
}
